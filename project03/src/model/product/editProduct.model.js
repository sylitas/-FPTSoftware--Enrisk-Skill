// import { DYNAMO_DB } from '../../libraries/const';
// import { query } from '../../libraries/dynamoDB';
// import { getEmailFromReq } from '../../libraries/util';

const editProduct = async () => {
  console.log('😎 Sylitas | Triggered successful function editProduct (Model)');
  return { data: { message: 'editProduct is triggered' } };
  // try {

  // } catch (error) {
  //   const message = error.message ? error.message : 'An error occurred at editProduct';
  //   return { error: { message } };
  // }
};

export default editProduct;
